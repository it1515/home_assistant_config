api_password: !secret http_password
base_url: !secret http_url
server_port: !secret secret_port
ssl_certificate: /ssl/fullchain.pem
ssl_key: /ssl/privkey.pem
ip_ban_enabled: true
login_attempts_threshold: 5